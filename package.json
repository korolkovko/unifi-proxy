{
  "name": "unifi-proxy",
  "version": "1.0.0",
  "description": "TLS SNI Proxy for Unifi firmware updates - bypasses geo-blocking",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "proxy",
    "tls",
    "sni",
    "unifi",
    "passthrough"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "pino": "^9.5.0",
    "pino-pretty": "^12.0.0"
  }
}
